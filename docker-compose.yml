version: '3.7'
services:
  bot:
    build: .
    command: python3 /usr/src/app/bot.py
    ports:
      - "8384:8384"
    # volumes:
    #   - /home/user/content:/content
    env_file:
      - docker/.env
    restart: always
